the MessageHelper Service Provider is responsible for:

providing the following services for other objects:
-	getSenderDisplayName(Address address)
-	toMe(Account account, Address[] toAddrs)
-	toFriendly(Address address, Contacts contacts)
-	toFriendly(Address address, Contacts contacts,
                                                 boolean showCorrespondentNames,
                                                 boolean changeContactNameColor,
                                                 int contactNameColor)
-	isSpoofAddress(String displayName)
-	isCompletePartAvailable(Part part)
-	createEmptyPart()

Managing System Configurations:
-	getRecipientDisplayNames(Address[] addresses):
-	getInstance(final Context context):
-	toFriendly(Address[] addresses, Contacts contacts):
-	getSenderDisplayName(Address address):

Offers services and configurations to the following objects
MessageHelper:
-	  isCompletePartAvailable(Part part)
-	  createEmptyPart()

